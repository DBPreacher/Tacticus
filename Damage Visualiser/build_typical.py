"""
build_typical.py - kept so old instructions still work.

typical-character.html is now written by build_map.py in the same run as the Roster Battle Map, because
the page uses every matchup table the map already works out (all tiers and settings). This just runs it.
"""
import build_map

if __name__ == '__main__':
    build_map.main()
